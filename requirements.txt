# Runtime dependencies for rectoverso.
# Test deps live in tests/requirements.txt.
#
# Install: pip install -r requirements.txt

# Anthropic SDK — Tier-3 Messages API + Tier-2 Managed Agents.
anthropic>=0.96.0

# Router / manifest loaders.
jsonschema>=4.20
PyYAML>=6.0

# Vertex AI Veo renderer (src/producer/veo.py) — lazy-imported ADC path.
# google-auth is the bearer-token provider. `requests` is the transport
# google-auth.transport.requests depends on; installing google-auth alone
# is not enough, the requests package must be present too.
google-auth>=2.25
requests>=2.31

# Upload endpoint for Editor artifact extraction (src/producer/upload_endpoint.py).
# Managed Agents sandbox has network egress but no inbound primitives for
# retrieving container-authored files; the agent curls large artifacts
# (MP4, composition.zip) to this local Flask endpoint, which ngrok exposes
# publicly for the duration of a dispatch. Werkzeug ships with Flask and
# handles the multipart parse without us owning that surface.
Flask>=3.0
